import { StyleSheet } from 'react-native';

const produtosStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    flexWrap: 'wrap',
    marginTop: 15,
  },
});

export default produtosStyles;
